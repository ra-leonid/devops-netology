apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: grafana
    app.kubernetes.io/name: grafana-web
    app.kubernetes.io/part-of: kube-prometheus
    app.kubernetes.io/version: 0.0.1
  name: grafana-web
  namespace: monitoring
spec:
  ports:
  - name: http
    port: 3000
    targetPort: http
    nodePort: 30080
  type: NodePort
  selector:
    app.kubernetes.io/name: grafana